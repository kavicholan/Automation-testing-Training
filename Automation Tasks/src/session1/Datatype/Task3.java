package session1.Datatype;
//Task 3: Write a Javaprogram that accepts two integers from the user and then prints thesum (add) , the difference (sub) and the product (mul).
public class Task3 {

    public static void main(String[] args) {
     int a= 10, b=20;
     int add,sub,mul ;

      add = a+b;
      sub = a-b;
      mul = a*b;

        System.out.println("Sum of Two Integer is " +add );
        System.out.println("Difference of Two Integer is" +sub);
        System.out.println("Product of Two Integer is " +mul);

    }
}
